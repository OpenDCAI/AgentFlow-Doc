diff --git a/node_modules/vuepress-theme-plume/lib/client/utils/index.js b/node_modules/vuepress-theme-plume/lib/client/utils/index.js
index 0000000..1111111 100644
--- a/node_modules/vuepress-theme-plume/lib/client/utils/index.js
+++ b/node_modules/vuepress-theme-plume/lib/client/utils/index.js
@@ -56,6 +56,7 @@ function resolveNavLink(link) {
 	};
 }
 function normalizeTitleWithPath(path) {
+	if (path == null || typeof path !== "string") return "";
 	path = path.replace(/index\.html?$/i, "").replace(/\.html?$/i, "").replace(/\/$/, "");
 	return decodeURIComponent(path.slice(path.lastIndexOf("/") + 1));
 }
@@ -78,6 +79,7 @@ const inBrowser = typeof document !== "undefined";
 }
 function isActive(currentPath, matchPath, asRegex = false) {
 	if (matchPath === void 0) return false;
+	if (currentPath == null || typeof currentPath !== "string") return false;
 	currentPath = normalize(`/${currentPath.replace(/^\//, "")}`);
 	if (asRegex) return new RegExp(matchPath).test(currentPath);
 	if (normalize(matchPath) !== currentPath) return false;
@@ -87,6 +89,7 @@ function isActive(currentPath, matchPath, asRegex = false) {
 	return true;
 }
 function normalize(path) {
+	if (path == null || typeof path !== "string") return "";
 	return decodeURI(path).replace(HASH_RE, "").replace(EXT_RE, "");
 }
 function numToUnit(value) {
